{
  "type": "object",
  "required": ["input", "format"],
  "properties": {
    "input": {"type": "string", "format": "file-path"},
    "format": {"type": "string", "enum": ["json", "csv", "xlsx"]},
    "dialect": {"type": "string", "enum": ["auto", "mysql", "postgresql", "sqlserver", "oracle", "sqlite"]},
    "include_constraints": {"type": "boolean"},
    "fail_on_unsupported": {"type": "boolean"}
  }
}
