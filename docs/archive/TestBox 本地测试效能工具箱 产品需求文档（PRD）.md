# TestBox 本地测试效能工具箱 产品需求文档（PRD）

版本：V1.0
产品名称：TestBox
产品类型：本地化、插件化测试效能工具
目标用户：测试工程师、自动化测试人员、测试开发人员

# 1. 产品概述

## 1.1 产品背景

在软件测试过程中，测试人员长期积累大量测试辅助工具，包括：

- 测试数据生成脚本
- SQL解析工具
- 数据校验工具
- 截图工具
- 测试留痕工具
- Excel处理工具
- 环境检查脚本

这些工具通常存在：

- 分散管理
- 使用入口不统一
- 参数配置复杂
- 无法复用
- 缺少统一规范

的问题。

TestBox旨在将测试过程中高频使用的工具进行标准化、插件化封装，形成个人及团队可持续沉淀的测试能力工具箱。

# 2. 产品定位

## 2.1 产品定义

TestBox 是一个：

> 面向测试工程师的本地化测试效能工具框架，通过统一CLI、GUI入口和插件机制，将测试数据、测试辅助、测试分析工具进行统一管理。

# 3. 产品目标

## 3.1 核心目标

实现：

- 一个入口管理所有测试工具
- 工具能力插件化扩展
- 测试资产持续沉淀
- 降低重复开发成本

# 4. 产品架构

```
                 TestBox

          CLI          GUI


               TestBox Core

------------------------------------------------

Plugin Manager

Config Manager

Task Engine

Log Manager

Report Manager


------------------------------------------------

Plugins


数据类

Data Generator

SQL Parser

Excel Tool


测试辅助类

Evidence Tool

Report Tool


环境类

Env Check


AI类

Case Generator

Log Analyzer
```

# 5. 核心模块需求

# 5.1 Core核心框架

## 功能

提供所有插件运行基础能力。

包括：

- 插件加载
- 生命周期管理
- 参数管理
- 配置管理
- 日志管理
- 输出管理

# 5.2 CLI入口

V1.0 统一命令：

```
testbox run <command> [--param value]
```

示例：

数据生成：

```
testbox run data.mock --count 100 --format csv --seed 10001
```

SQL解析：

```
testbox run sql.parse --input ./example.sql --format xlsx
```

测试留痕：

```
testbox run evidence.capture --steps ./steps.yaml
```

插件管理与任务查询命令由 Core 技术设计文档第 23.2 节定义。GUI 在后续版本复用同一命令和任务 API。

# 5.3 GUI入口

后续支持桌面客户端。

功能：

- 工具列表
- 参数配置
- 执行按钮
- 日志展示
- 结果查看

技术：

PySide6

# 6. 第一批插件需求

# 6.1 Data Generator 测试数据生成插件

## 产品定位

生成满足测试要求的模拟数据。

## 功能范围

### 个人信息生成

支持：

- 中文姓名
- 性别
- 年龄
- 出生日期
- 地址

### 身份证模拟生成

支持：

- 测试标识符格式生成（不可用于真实身份认证）
- 使用明确的测试前缀或故意无效校验位
- 年龄关联
- 性别关联

支持输出：

- JSON
- Excel
- SQL

### 身份证图片生成

生成测试证件图片：

包含：

- 正反面模板
- 模拟信息
- 测试水印

要求：

必须包含：

```
TEST DATA
测试数据
```

禁止生成真实个人证件，也禁止生成可通过真实身份认证校验的号码或图像。

### 手机号码生成

支持：

- 手机号格式生成
- 批量生成
- 前缀配置

### 图片数据生成

支持：

- 用户头像
- 签名图片
- 流程附件图片

### 银行业务数据

支持：

客户：

- 客户号
- 账户
- 交易流水
- 产品信息

## 输出格式

支持：

- SQL
- CSV
- Excel
- JSON
- ZIP测试数据包

# 6.2 SQL Parser插件

## 功能

解析SQL文件。

支持：

- CREATE TABLE解析
- 字段提取
- 类型识别
- 分区识别
- 注释提取

输出：

Excel字段清单。

SQL Parser 首期兼容 MySQL、PostgreSQL、SQL Server、Oracle、SQLite 的常见 DDL 表达方式，支持嵌套类型括号、不同标识符引用、表级/字段级主键、唯一、外键、自增、默认值和注释；解析不支持内容时输出结构化警告，可配置为严格失败。插件不得连接数据库或执行输入 SQL。

# 6.3 Evidence Tool 测试留痕插件

## 产品定位

将测试过程中的操作、截图、验证结果形成测试证据。

## 功能

截图：

- 页面截图
- 区域截图
- 浏览器截图

留痕：

记录：

- 测试步骤
- 操作说明
- 结果
- 图片附件

输出：

```
测试留痕报告.xlsx

测试证据.zip

HTML报告
```

# 6.4 Excel Tool

功能：

- Excel生成
- 模板填充
- 数据转换
- 测试报告生成

# 6.5 Env Check

功能：

检查：

- 网络
- 服务
- 数据库连接
- 文件权限

# 7. 非功能需求

## 性能

- CLI启动 < 3秒
- 单次生成10万测试数据满足常规测试需求

## 扩展性

新增插件：

无需修改Core。

## 安全

要求：

- 密码禁止明文保存
- 配置支持加密
- 输出文件支持清理
- 插件按最小权限运行；未知来源插件不得安装
- 身份类数据只能生成不可用于真实认证的测试标识符

# 8. 版本范围与优先级

## 8.1 V1.0 交付范围

V1.0 只交付可本地运行的 CLI 与插件运行框架，优先验证“发现插件—执行命令—产出可追溯结果”的完整闭环。

| 优先级 | 能力 | V1.0 要求 |
| --- | --- | --- |
| P0 | Core 与 CLI | 发现、校验、加载本地插件；列出并执行命令；统一返回码与结果文件 |
| P0 | Data Generator | 生成可复现的模拟个人信息和手机号，导出 JSON、CSV、Excel |
| P0 | SQL Parser | 解析常见 `CREATE TABLE`，导出字段清单 |
| P0 | 任务追踪 | 为每次运行创建独立工作区、日志、`result.json` 与 Markdown 报告 |
| P1 | Excel Tool | CSV/Excel 转换与模板填充 |
| P1 | Env Check | 网络、端口、文件写入权限检查 |
| P2 | Evidence Tool | 截图采集与证据包；仅定义接口，暂不承诺浏览器自动化能力 |
| P2 | GUI、插件市场、AI 插件 | GUI 的设计规范在 V1.0 固化，具体桌面客户端与其他能力不属于 V1.0 交付范围 |

## 8.2 典型用户流程

1. 用户执行 `testbox plugin list` 查看已安装插件与可用命令。
2. 用户执行例如 `testbox run data.mock --count 100 --format csv --seed 10001`。
3. Core 校验命令和参数，创建本次任务工作区，并将执行交给对应插件。
4. 终端输出简要结果；完整日志、附件、`result.json` 与报告保存在工作区。
5. 用户可使用任务 ID 查看历史记录或清理已过期工作区。

## 8.3 验收标准

| 场景 | 验收结果 |
| --- | --- |
| 启动与发现 | 空插件目录可启动；无效清单不会导致其他有效插件不可用，并能显示原因 |
| 命令执行 | 已注册命令能接收参数、返回成功/失败状态，进程返回码与状态一致 |
| 可复现 | 相同插件版本、参数与 `seed` 在同一环境生成相同结构和内容 |
| 可追溯 | 每次运行都有唯一任务 ID、开始/结束时间、脱敏参数、日志、结果和附件路径 |
| 数据安全 | V1.0 不读取或生成真实个人身份信息；所有证件类演示素材具有不可移除的测试水印 |
| 失败处理 | 参数、输入文件、插件运行错误均给出可理解的错误信息，且不打印密钥或敏感配置 |

## 8.4 非目标与约束

- 不提供云端同步、多人协作或远程插件执行。
- 不承诺兼容任意方言 SQL；首期支持范围以测试用例中列出的方言为准。
- 不将真实生产数据作为输入、样例、日志或报告内容。
- GUI 必须复用 Core 的命令与任务 API，不得复制插件执行逻辑。

# 9. 成功度量

首期试用以以下指标评估：CLI 冷启动在目标开发机上不超过 3 秒；P0 命令端到端成功率不低于 95%；每个 P0 插件至少有正常、参数错误和文件错误三类自动化测试；试用用户可在不编写脚本的情况下完成至少一次数据生成和一次 SQL 字段清单导出。

# 10. GUI 设计约束

GUI 不是第二套业务实现，而是 Core 命令、参数 Schema、任务状态与 Result 的可视化入口。界面信息架构、组件、状态、可访问性和动效约束见《TestBox UI 设计规范》；GUI 验收必须使用同一插件与相同参数调用 Runtime，并显示同一个任务 ID、日志、结果和附件。

# 11. Data Generator 规则化扩展

Data Generator 从固定个人信息输出演进为字段规则驱动的模拟数据生成器。输入支持 SQL DDL、Excel 字段清单、JSON 规则集和金融模板；用户规则优先于字段注释推断、字段名语义和类型默认规则。

- 字段可独立启用/关闭，并支持生成器、长度、精度、枚举权重、日期区间、格式模板与派生规则。
- 唯一性默认关闭；用户可对单字段或复合字段显式启用，仅保证当前任务内唯一。容量不足时失败而不输出重复值。
- 地址默认全国随机；内置资源覆盖全部省级行政区，支持省、市、区县筛选与版本化数据更新。
- 手机号默认覆盖常用中国大陆号段，支持号段筛选与字段级唯一性；不得声称号码真实有效或可联系。
- 首批金融模板为客户、账户、产品、交易，提供客户类型、风险等级、账户类型、金额、币种、渠道、状态和时间等默认规则；用户可创建具名、版本化规则集复用。
- 不支持跨任务唯一性，不读取真实生产数据，不生成可用于真实认证的金融凭证或身份材料。

Data Generator 必须支持 JSON、CSV、XLSX、TXT、SQL 和 ZIP 数据包输出。TXT 支持分隔符、表头、编码与字段顺序；SQL 支持 MySQL、PostgreSQL、SQL Server、Oracle、SQLite 的批量 `INSERT`，并必须正确处理标识符、空值、布尔值、数值、日期和单引号转义。ZIP 可组合多种格式并包含生成摘要与规则版本。

行政区划资源可引用开源数据，但必须在插件中保留固定提交、来源 URL、许可证文本、许可证标识和文件 SHA-256。当前地址资源引用 `modood/Administrative-divisions-of-China` 的 `c49d495` 提交，许可证为 WTFPL-2.0。
