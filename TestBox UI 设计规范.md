# TestBox UI 设计规范

版本：V1.0

# 1. 目的与边界

本规范为未来桌面 GUI 建立统一的交互与视觉约束。GUI 是 TestBox Runtime 的展示层，不得包含插件执行、参数转换、文件写入或任务状态机的复制实现。所有执行必须调用 Core，所有页面均以 `task_id`、Schema、Result 和任务事件为唯一事实来源。

设计目标是让测试工程师在高信息密度的桌面场景中感到清晰、可控、可追溯：先完成常用任务，再按需展开高级信息。

# 2. 信息架构

一级导航固定为：

| 区域 | 内容 | 主要动作 |
| --- | --- | --- |
| 工具 | 已安装插件及命令 | 选择命令、查看说明、开始任务 |
| 任务 | 正在运行与历史任务 | 查看进度、取消、打开结果 |
| 工作区 | 输入、输出、报告与附件 | 打开位置、复制路径、清理过期内容 |
| 插件 | 插件状态、版本、能力和校验结果 | 验证、启用、禁用、查看诊断 |
| 设置 | 非敏感偏好与配置引用 | 修改外观、路径和默认限制 |

常用路径为“工具 → 填写参数 → 运行 → 任务详情 → 结果/报告”。不要把高级配置、原始日志或插件诊断放入首次执行页面；它们应在可展开区域或详情页中呈现。

# 3. 统一页面与状态

每个执行页都必须包含：命令名称与插件版本、参数表单、参数说明、运行按钮、预期输出位置，以及最近任务摘要。参数表单完全由 `input_schema` 生成；无法表达的复杂参数使用 JSON 编辑器，并在提交前显示结构化校验错误。

任务详情固定展示：状态、任务 ID、开始/结束时间、插件及版本、脱敏参数、实时日志、进度、输出文件、报告和错误建议。状态颜色和文案全局一致：

| 状态 / 结果 | 颜色语义 | 文案 |
| --- | --- | --- |
| PENDING / RUNNING | 中性蓝 | 等待中 / 运行中 |
| SUCCEEDED | 绿色 | 已完成 |
| FAILED / ABANDONED | 红色 | 失败 / 意外中断 |
| CANCELLED | 中性灰 | 已取消 |
| `Result.warnings` 非空 | 琥珀色 | 已完成，含警告 |

颜色不能作为唯一信息载体；每个状态同时使用文字和图标。运行中只允许“取消”这一破坏性操作；清理工作区、禁用插件等不可恢复操作必须说明影响范围并提供确认。

# 4. 视觉系统

- 使用系统字体和平台原生控件习惯；桌面正文基准为 14–16px，代码、路径和任务 ID 使用等宽字体。
- 采用 4px 间距基线；常用间距为 8、12、16、24、32px。表单标签与控件保持固定对齐，避免高密度页面跳动。
- 建立语义色 Token：`accent`、`success`、`warning`、`danger`、`text-primary`、`text-secondary`、`surface`、`border`，禁止插件自定义全局主题色。
- 默认使用浅色/深色双主题；对比度至少满足 WCAG AA。不要使用大面积渐变、自动播放背景或与任务无关的装饰动画。
- 卡片只用于分组，避免“卡片套卡片”。侧栏、工具栏和浮层可使用轻度半透明材质，但在降低透明度或高对比偏好下必须变为实色。

# 5. 交互与动效

交互优先即时反馈：点击后立刻呈现按下态，表单校验在字段附近显示，运行按钮提交后立即切换为可取消的运行状态。默认过渡采用短暂、无回弹的淡入或位移（约 200–400ms）；不得因为动效阻塞输入。

抽屉、详情面板和弹窗的进入与退出方向必须一致，并锚定触发控件。启用“减少动态效果”时，所有位移和弹性动效替换为短暂淡入淡出；启用“降低透明度”或“提高对比度”时，相应提升表面不透明度和边框对比度。

# 6. 可访问性与键盘

- 全部功能可使用键盘访问；焦点顺序与视觉顺序一致，焦点指示始终可见。
- 提供快捷键并在菜单中展示；不得劫持系统快捷键。建议：`Cmd/Ctrl+Enter` 运行、`Esc` 取消当前弹层、`Cmd/Ctrl+K` 打开命令搜索。
- 图标按钮必须有可访问名称和悬停说明；错误信息必须说明原因、位置和修复方式，不能只显示错误码。
- 支持系统文字缩放、深浅色模式、减少动态效果、降低透明度和提高对比度偏好。

# 7. 组件治理与验收

组件库至少统一：按钮、表单字段、选择器、文件选择、命令搜索、状态徽标、进度、日志查看器、表格、空状态、错误提示、确认对话框和通知。插件只能提供 Schema、描述、图标与局部内容，不能注入任意样式、脚本或全局导航。

每个 GUI 功能的验收必须覆盖：空状态、加载/运行状态、成功、警告、失败、取消、权限拒绝、键盘操作和深浅色主题。界面原型应先使用真实任务事件验证，再进入正式实现；UI 测试需断言显示的任务状态与 Core 返回结果一致。
