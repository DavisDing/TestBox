# 项目协作入口
- 开始任务先读取 docs/AI_CONTEXT.md，了解已核实的项目事实与特殊约束。
- 涉及需求分析、设计或实现时，再读取 docs/REQUIREMENT.md 与 docs/DESIGN.md。
- 上述文件缺失、未填写或包含示例时，不得将占位内容当作已确认事实。
- REQUIREMENT 定义需求与验收；DESIGN 定义当前实现方案；AI_CONTEXT 记录长期现状。
- 文档与代码不一致时，区分目标与现状；影响本次工作的冲突标记 NEEDS_CONFIRMATION。
- 不因代码与文档不同就擅自改变需求；阻塞性问题确认后再继续相关修改。
- 按当前会话指定的角色和范围工作，不自行跨入下一阶段。
- 已确认 UI 的保护范围以项目文档为准；必要变更先说明原因与影响。
- 若存在未提交改动，保护用户已有工作，不覆盖或回退不属于本次任务的修改。
- 使用 AI_CONTEXT 中已核实的构建与验证命令；缺失时从项目配置核实，不编造结果。
- 长期事实变化时提出 AI_CONTEXT 更新建议；临时进度留在会话或交接记录中。
